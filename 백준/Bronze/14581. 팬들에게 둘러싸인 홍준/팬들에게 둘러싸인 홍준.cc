#include <iostream>
#include <string>
using namespace std;

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	string name;
	cin >> name;	
	cout << ":fan::fan::fan:\n";
	cout << ":fan::" << name << "::fan:\n";
	cout << ":fan::fan::fan:\n";
	return 0;
}