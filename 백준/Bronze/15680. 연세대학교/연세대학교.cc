#include <iostream>
using namespace std;

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	int n;
	cin >> n;
	if (n == 1) cout << "Leading the Way to the Future";
	else cout << "YONSEI";
	return 0;
}